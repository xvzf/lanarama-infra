(base) {
    log
    errors
}

. {
    forward . 1.1.1.1 8.8.8.8
    prometheus localhost:9153
    import base
}

lanarama.local {
    hosts /etc/dns_internal_hosts
    prometheus localhost:9253
    import base
}